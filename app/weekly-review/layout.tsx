import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Review tuần — Gia Anh Furniture' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
