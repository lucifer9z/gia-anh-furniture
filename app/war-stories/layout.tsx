import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'War Stories — Gia Anh Furniture' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
