import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Nhập số liệu — Gia Anh Furniture' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
