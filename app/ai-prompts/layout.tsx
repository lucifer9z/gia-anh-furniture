import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'AI Prompts — Gia Anh Furniture' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
