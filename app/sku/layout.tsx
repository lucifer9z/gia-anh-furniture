import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SKU Tracking — Gia Anh Furniture' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
